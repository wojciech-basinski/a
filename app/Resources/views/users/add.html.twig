{% extends 'base.html.twig' %}


{% block container %}
    <div class="panel panel-info">
        <div class="panel-heading">
            <h3 class="text-center">Dodaj użytkownika</h3>
        </div>
        <div class="panel-body">
            {% include 'template/flashMessages.html.twig' %}
            {{ form(form) }}
            <a href="{{ path('users_all') }}" class="btn btn-success">Powrót</a>
        </div>
    </div>
{% endblock %}